// Завершите метод,
//  который принимает логическое значение и возвращает "Yes"строку для true, 
//  или "No"строку для false.

function boolToWord( bool ){
    if(bool==true){
      return `Yes`;
    }else {
      return `No`;
    }
  }